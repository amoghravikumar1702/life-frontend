import IndustrySelection from "@/components/onboarding/IndustrySelection";

export default function OnboardingPage() {
  return (
    <main className="min-h-screen flex items-center justify-center bg-[#0B0B0C] p-8">
      <IndustrySelection />
    </main>
  );
}